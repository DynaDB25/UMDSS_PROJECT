# Full backend dependency set. The venv previously had only Django + CORS
# installed, so the scraper could not import at all.
#
# Install with:  .venv/Scripts/python.exe -m pip install -r requirements.txt
# Playwright also needs its browser binaries:  python -m playwright install chromium

# Core
Django==5.2.16
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
cryptography==49.0.0

# Task queue (settings default to CELERY_TASK_ALWAYS_EAGER, so a broker
# is only required when running a real worker)
celery==5.6.3
redis==8.0.1

# Scraping
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
selenium==4.46.0
webdriver-manager==4.1.2
playwright==1.61.0
psycopg2-binary==2.9.12
dj-database-url==3.1.2